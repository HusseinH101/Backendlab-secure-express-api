# Backendlab-secure-express-api
Backend lab with a Node.js Express application built using the MVC pattern that provides a REST API and web interface for managing tasks, with database storage, JWT authentication, API key protection, and EJS-based pages.
